import { profile } from 'console';
import React from 'react';

export const Profile = () => {
  return (
    const profile = (
    <div>
      Profile Section
    </div>
    );
    return profile;
  )
};